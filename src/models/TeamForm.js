export default class TeamForm {
  constructor(teamname, teamTitle, teamImg, category) {
    this.teamname = teamname;
    this.teamTitle = teamTitle;
    this.teamImg = teamImg;
    this.category = category;
  }
}
